import { ComingSoonStub } from "../_coming-soon";

export default function HqOnboardingPage() {
  return (
    <ComingSoonStub
      title="Onboarding & migration"
      sprint="HQ-4"
      body="The CEO view of where each customer is in their onboarding — payment status, migration %, current stage, expected go-live, assigned owner — ships in HQ-4. Customers already see their own setup-checklist on /dashboard (live since PR #63); HQ-4 surfaces the same data cross-tenant."
    />
  );
}
