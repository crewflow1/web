import { ComingSoonStub } from "../_coming-soon";

export default function HqSupportPage() {
  return (
    <ComingSoonStub
      title="Support queue"
      sprint="HQ-5"
      body="OPEN · IN PROGRESS · WAITING · RESOLVED ticket queue with Reply / Assign / Resolve / Open company / Impersonate buttons. Lands in HQ-5 alongside Alerts and Internal notes — they share the same notifications + audit-log spine."
    />
  );
}
