import { ComingSoonStub } from "../_coming-soon";

export default function HqBillingPage() {
  return (
    <ComingSoonStub
      title="Billing"
      sprint="HQ-4"
      body="Setup-fee tracking + monthly status + outstanding balances + Send-setup-payment / Mark-paid / Resend-invoice / Pause / Cancel / Refund actions. NO automatic charging until you say so — HQ-4 ships status tracking architecture only. Statuses: SETUP SENT · SETUP PAID · ACTIVE (ONBOARDING) · ACTIVE · PAST DUE · CANCELLED."
    />
  );
}
