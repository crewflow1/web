import { ComingSoonStub } from "../_coming-soon";

export default function HqAnalyticsPage() {
  return (
    <ComingSoonStub
      title="Analytics"
      sprint="HQ-6"
      body="Landing → Demo → Won → Payment → Onboarding → Active funnel with conversion percentages. Data is already computable from demo_requests + organizations + the new billing columns — the UI just needs the funnel render. Lands in HQ-6."
    />
  );
}
