# Roadmap Completion — WAVE A (Correctness + Security/Hygiene + Type Safety + Zero-Customer Cost)

Execution ledger. Wave A only: fix known correctness/engineering defects. NO new roadmap breadth, NO provider activation, NO autonomous AI, NO UI redesign, NO Wave B/C.

## Rule Zero — authoritative starting state (2026-08-23)
- origin/main SHA: **d38c9992** ("infrastructure sizing #837")
- production SHA: **d38c999** = main (live), /api/health = healthy, db:ok, comms.email=true, sms/whatsapp/weather dark
- product/ux-rebuild (PR #838): **1ea34993** DRAFT — SEPARATE, DO NOT TOUCH
- Prod DB: **373 applied, tip 20261213** == 373 migration files → exact parity
- Branch: `roadmap/wave-a-correctness` @ d38c9992 (worktree /Users/moetalibi/Code/web-wave-a)
- Next free migration prefix: **20261214**

## Central migration allocation (coordinator-owned — agents MUST NOT allocate)
| Prefix | Purpose | Train |
|---|---|---|
| 20261214000000 | audit_log_immutability (append-only triggers) | A.5 |
| 20261215000000 | revoke_default_grants (least-privilege) | A.6 |
| 20261216000000 | (reserve if A.1 needs schema) | A.1 |

## Findings verification + status
| # | Finding | Verified status | Root cause | Fix | Migration | Review | Deployed |
|---|---|---|---|---|---|---|---|
| A.1 | alerts-poll + hq-decision-autopropose 100% fail (demo_requests.org_id) | **CONFIRMED → FIXED** | hq-alerts-snapshot.ts queried demo_requests.org_id + ordered by updated_at; NEITHER column exists (demo_requests is a GLOBAL funnel; links via linked_org_id; row stamp is approved_at). Both crons route through buildAlertsSnapshot() so one fix covers both. | select alias org_id:linked_org_id; filter .in("linked_org_id",orgIds); order by created_at; toAlertDemo uses approved_at. Proven vs real prod (old=42703, new=OK). | none | pending |
| A.2 | VAT cash scheme: input VAT (Box 4) accrual not cash | **CONFIRMED** | compute.ts box4 summed finances.vat_total by created_at regardless of scheme → cash-scheme org reclaims input VAT on UNPAID bills = underpays HMRC. Data model supports cash basis (supplier_payments.paid_at + allocations.amount + bill vat_total). | compute: accumulateInputVat() cash-apportions box4 on supplier payment paid_at; accrual fallback when no ledger. **+ WIRING (in progress): gatherVatQuarterInputs must pass the ledger for cash orgs (else dormant); + box6/7 consistency.** 16+ fixtures; standard/FRS/RC unchanged. | ADVERSARIAL — pending (full path) | pending |
| A.3 | types.ts only 45/306 tables | **CONFIRMED → FIXED** | 45→315 tables regenerated (`supabase gen types typescript --linked`). Drift root cause: no gen script + no CI guard. | Regenerated types.ts (315 tables). ONE genuine error surfaced (jobs/actions.ts:130 clone_job_template p_anchor_date — gen-types types a defaultless nullable-accepting param as non-null; narrow documented cast, body handles null; follow-up: add `default null`). Added `db:types` npm script + deterministic no-DB coverage guard (__tests__/db/types-table-coverage.test.ts). **SIDE EFFECT:** complete types.ts now names every table+RPC → 43 source-contract guards (receptionist-* "exactly one module names X") counted the GENERATED file; excluded `/lib/supabase/types.ts` from ~58 application-module walkers (path-anchored; invariant preserved, NOT weakened). typecheck 0; security 8146/8146; unit 10831/10831. | n/a | pending |
| A.4 | cron_runs bloat (dark crons write telemetry pre-gate) | **CONFIRMED (audit framing CORRECTED)** | Most audit-named crons are NOT dark — they do legit per-tick work (hq-exec/roster/runners-tick enqueue+drain real HQ tasks; spine-drain row IS the health golden-signal; task-reaper/automation/cadence real reconciliation). Only 2 genuinely-dark no-op writers: hq-apply-drain (kill-switch off, 288/day) + memory-embed (no provider, 720/day). Backlog (146k rows/171MB, only 3k >14d) is recent LEGIT telemetry bounded by 14d retention (working, ≥8d floor by design). | hq-apply-drain 204-before-telemetry (D) + memory-embed 204-before-telemetry via existing isEmbeddingConfigured() (coordinator; ai-governance guard stays green 38/38). Retention window LEFT (deliberate safe design; shortening = "make metrics look better", cautioned against). ~1,008 rows/day (~30k/mo) dark writes eliminated; Vercel invocations unchanged (scheduling untouched). memory-embed follow-up from D subsumed. | none | pending |
| A.5 | activity_log/admin_activity_log mutable by service_role | **CONFIRMED → FIXED (review pending)** | prod: only AFTER-INSERT notify trigger; both mutable by service_role; no append-only enforcement | mig 20261214: BEFORE UPDATE/DELETE triggers RAISE append-only for ALL roles (ai_invocations idiom); freeze event-fact cols; sanctioned exceptions preserved (GDPR anonymise PII-only; retention purge via crewflow.audit_maintenance marker; org-cascade teardown probe; admin actor_id→null). 12 integration tests. | ADVERSARIAL a54d98f2 running | pending |
| A.6 | latent default grants on service-role-only tables | **CONFIRMED → FIXED (review pending)** | prod: GRANT ALL to anon+authenticated (incl TRUNCATE) on impersonation_sessions/activity_log/admin_activity_log/webhook-events/17 receptionist_* — inert only via RLS | mig 20261215: explicit REVOKE per table (evidence-based, zero-policy tables only); activity_log keeps authenticated SELECT; webhook_endpoints/deliveries left (legit RLS); introspection RPC for guard. 5 integration tests. | ADVERSARIAL a54d98f2 running | pending |
| A.7 | CI: no secret-scan/dependabot/dup-prefix-guard; npm audit advisory | **CONFIRMED → FIXED** | gaps real (only ci.yml; no dependabot/secret-scan) | gitleaks security-scan.yml (SHA-pinned, separate workflow, free/public, no Vercel) + .gitleaks.toml narrow allowlist; dependabot.yml (npm+actions weekly grouped capped); dup-migration-prefix test (373 unique PASS); npm audit kept advisory w/ documented policy (docs/roadmap/A7-CI-SUPPLY-CHAIN-HYGIENE.md); CodeQL/rollback/hard-audit skipped+justified. ci.yml UNTOUCHED (workflow-gates pin 19/19). | none | n/a | pending |
| A.8 | small findings | **ALL VERIFIED → DEFER** | (1) NI-cat-A on-costs CONFIRMED (job-cost-input.ts:170, no niCategoryByUser) — conservative/safe-direction, not filing-affecting → DEFER; (2) CIS email idempotency CONFIRMED but already an in-code documented follow-up (cis-statement-emails.ts:44-52), low/bounded/admin → DEFER; (3) supplier due date = data-model gap → DEFER; (4) portal token cleartext + expires_at:null CONFIRMED, NOT an isolation break (122-bit UUID), expiry is product/UX → DEFER; (5) request-ids exist but not persisted to audit → DEFER; (6) v1 POST no Idempotency-Key CONFIRMED, public API dark-gated → DEFER | none this wave | n/a | n/a |

## Adversarial reviews
- **E (A.5/A.6) security review (a54d98f2): VERDICT SAFE-TO-DEPLOY.** Real gains confirmed (UPDATE history-rewrite blocked for all roles incl service_role; latent client GRANT ALL/TRUNCATE on 22 tables removed; REVOKE scope correct no under/over-reach; purge re-create byte-faithful; introspection RPC safe; no legit-write regression). Two hardening findings → sent back to E: P1 TRUNCATE untriggered + marker ungated (service key can still erase — not a regression); P2 activity_log UPDATE leaves actor_id/metadata mutable (broader than idiom). FIXING: add BEFORE TRUNCATE triggers + tighten activity_log UPDATE to GDPR-scrub-only + reword over-claim.
- **B (A.2) financial review (aa302c65): VERDICT SAFE-TO-DEPLOY.** All 10 checks CONFIRMED: unpaid→box4=£0 (counterfactual £20); box4 ≤ cash paid (no under-payment/over-reclaim); allocation.amount is GROSS incl CIS (full VAT reclaimed on CIS bills); box4+box7=cash paid one window; single round2 (no drift); RC unchanged+not double-counted; standard/FRS unchanged; org-pinned isolation; credit-notes/neg/÷0 guarded; cash-out.ts accrual is a flagged estimate, filed return uses wired prepareVatReturn. Non-blocking S2 follow-up: unpaid reverse-charge bill → £0 box7 NET (informational box, net-neutral for VAT due). No filing-affecting defect. Wired proof: unpaid purchase → box4 £0 via real gatherVatQuarterInputs. 728 tax/integration tests green.

## Deploy log
(append)

## Lessons
(append)
